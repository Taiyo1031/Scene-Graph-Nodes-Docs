---
title: Evaluation Errors
---

Errors are stored on the node and on the node tree. Evaluation continues after per-node errors, while cycles stop graph evaluation.

## Maintenance

Update this page whenever the corresponding code surface changes. Run `npm run check-docs` before publishing documentation updates.
